{
  "name": "hello-worlds",
  "author": "Kenneth Pirman <github@kenny.wtf>",
  "description": "Monorepo for Hello-Worlds – Your Virtual Worldbuilding toolkit",
  "version": "0.0.0",
  "private": "true",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ship:worlds": "flyctl deploy ./ -c apps/planet/fly.toml --dockerfile=./apps/planet/Dockerfile",
    "ship:docs": "flyctl deploy ./ -c docs/fly.toml --dockerfile=./docs/Dockerfile"
  },
  "dependencies": {
    "@changesets/cli": "^2.23.2"
  }
}

import * as React from "react";
import { useNoiseController } from "../noise/NoiseController";

const Planet: React.FC = () => {
  const noise = useNoiseController();
  return null;
};

export default Planet;
